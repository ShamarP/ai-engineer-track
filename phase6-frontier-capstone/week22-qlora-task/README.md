# Week 22 — QLoRA fine-tune

**Why this week:** most fine-tuning is a waste of time vs. better prompting. The week's purpose is to learn *when* it isn't, and execute it competently.

## 📖 Read (~5 hours)
- [ ] **AIE Ch 7** — *Finetuning*; **Ch 8** — *Dataset Engineering*
- [ ] **Hu et al., 2021, *LoRA*** ([arxiv 2106.09685](https://arxiv.org/abs/2106.09685)). Sections **1**, **3**, **4**.
- [ ] **Dettmers et al., 2023, *QLoRA*** ([arxiv 2305.14314](https://arxiv.org/abs/2305.14314)). Sections **1**, **3**, **4**.
- [ ] **Rafailov et al., 2023, *Direct Preference Optimization (DPO)*** ([arxiv 2305.18290](https://arxiv.org/abs/2305.18290)). Sections **1**, **3**, **4**.
- [ ] **HuggingFace `trl` and `peft` docs**

Note files: `notes/papers/hu-2021-lora.md`, `notes/papers/dettmers-2023-qlora.md`, `notes/papers/rafailov-2023-dpo.md`.

## 🧠 Concepts to internalize
- [ ] LoRA: freeze the base, train low-rank adapters at every attention layer
- [ ] QLoRA: load base in 4-bit, train LoRA in fp16. Lets you fine-tune 70B on a single 80GB GPU.
- [ ] DPO: fine-tune directly on preference pairs without a separate reward model. Replacing PPO in 2024+.
- [ ] Dataset construction is 80% of fine-tuning success. Garbage in = garbage out.
- [ ] When to fine-tune vs. prompt: tone/style, narrow domain, latency-sensitive, ≥1k high-quality examples
- [ ] When *not* to: knowledge updates (use RAG), most one-off tasks (prompt is faster)

## 🛠 Build
1. [ ] Identify a real task from your week-4 prompt playground where the best prompt still loses ≥30% of the time
2. [ ] Build a 1k-example training set + 200-example eval set (frontier model labels, hand-fix 20%)
3. [ ] QLoRA fine-tune `Llama-3.1-8B-Instruct` on the training set (Modal, RunPod, or Lambda — one A100 hour)
4. [ ] Compare prompt-only baseline vs. fine-tuned, on the eval set, with your week-7 eval harness
5. [ ] Bonus: try DPO on top of SFT — 200 preference pairs (chosen/rejected) generated by hand-judging
6. [ ] README: include the win-rate table; honest report

## ✅ Done when
- [ ] Fine-tuned 8B beats prompt-only frontier model on your task by ≥10 pts
- [ ] You can explain LoRA vs. QLoRA vs. full fine-tune and when each is the right call

## 🎯 Stretch
- [ ] Deploy your fine-tuned model on Modal or Replicate; add it to your week-15 Rust proxy as another backend.

---

## What I built

## What I learned

## What I got wrong
